#!/usr/bin/env ruby

require 'bundler/setup'
require 'evolvable_sound'

# You can add fixtures and/or initialization code here to make experimenting
# with your gem easier. You can also use a different console, if you like.

# (If you use this, don't forget to add pry to your Gemfile!)
# require "pry"
# Pry.start

require 'irb'

# population_attrs = {
#   name: Time.now.utc.to_i,
#   size: 10,
#   mutation: Evolvable::Mutation.new(rate: 0.2),
#   log_progress: true
# }
# population = EvolvableSound.evolvable_population(population_attrs)
# puts population.name
# population.evolve!(generations_count: 10_000)

# population_name = ''
# EvolvableSound.compose_song(population_name)
# EvolvableSound.play_song(population_name)

IRB.start(__FILE__)
